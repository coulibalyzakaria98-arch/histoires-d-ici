{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ],
  "installCommand": "npm ci || npm install",
  "buildCommand": "npx --yes vite build",
  "outputDirectory": "dist",
  "framework": "vite"
}
