{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "npm install --no-package-lock && npm run build",
  "outputDirectory": "dist",
  "framework": "vite"
}
